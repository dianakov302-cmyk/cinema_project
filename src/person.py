from abc import ABC, abstractmethod

# 🔸 Абстракція: створюємо абстрактний клас Person, який задає загальну форму для всіх людей (актор, режисер)
class Person(ABC):
    def __init__(self, name, age):
        # 🔸 Інкапсуляція: приховуємо дані (можна додати _name, _age, щоб позначити, що це внутрішні змінні)
        self.name = name
        self.age = age

    # 🔸 Поліморфізм: метод get_role() буде реалізований по-різному в Actor і Director
    @abstractmethod
    def get_role(self):
        pass

    def __str__(self):
        return f"{self.name}, {self.age} років"
